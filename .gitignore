# Variables de entorno — NUNCA subir al repo
.env
.env.local
.env.*.local

# Exportación de datos reales de Firestore (contiene PII de clientes) y
# credenciales de service account — nunca deben subirse al repo
migrate-export/
*firebase-adminsdk*.json

# Config local de Claude Code (puede contener comandos con credenciales en texto plano)
.claude/

# Dependencias
node_modules/

# Build Vite
dist/
dist-ssr/

# Cloud Functions build output
functions/lib/

# Archivos de sistema
.DS_Store
Thumbs.db

# Netlify
.netlify/

# Firebase / Vercel (estado local de CLI, no debe subirse)
.firebase/
.vercel/
